pipeline {
    agent any
    stages {
        stage('Stage') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'nsxcreds', passwordVariable: 'pass', usernameVariable: 'user')]) {
                }
            }
        }
    }
}
